server:
  port: 8080

spring:
  main:
    web-application-type: reactive
  data:
    mongodb:
      database: users_db
      uri: mongodb+srv://default:bRxyDvHGcaXgzCH8@tsg-alpha-db.istomwf.mongodb.net/
    redis:
      host: localhost
      port: 6379
  cache:
    type: redis
  rsocket:
    server:
      port: 6565
      transport: websocket
      mapping-path: /rsocket

redis:
  timeout: 2000

jwt:
  token:
    timeout: 1800000
    refreshTimeout: 604800000
    key: sam's silly little key